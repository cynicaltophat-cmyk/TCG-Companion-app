# Gemini API Key for card analysis and identification
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase configuration (can be provided via individual vars if needed, 
# but currently read from firebase-applet-config.json which is gitignored)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_PROJECT_ID=
# ...
